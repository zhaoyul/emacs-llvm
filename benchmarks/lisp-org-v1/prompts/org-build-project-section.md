Extend `notes.org` without deleting its existing title or introduction.

Create a top-level heading `Project Alpha` with property `OWNER` set to `Kevin`. Under it, create `Metrics` and `Computation` child headings. Add one Org table under `Metrics` that records an `Answer` value of `42`. Add one `emacs-lisp` Babel block under `Computation` that evaluates `(+ 40 2)`, then execute it so the document contains the result `42`.

Keep the Org hierarchy valid and do not modify `DO_NOT_TOUCH.txt`.
