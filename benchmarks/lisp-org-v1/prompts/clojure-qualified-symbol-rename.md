Rename the Clojure function `bench.math/sum` to `bench.math/add`.

Update executable qualified references such as `math/sum`, while preserving the alias and namespace. Do not alter the string or comment containing `math/sum`. Keep both namespaces loadable and do not modify `DO_NOT_TOUCH.txt`.
