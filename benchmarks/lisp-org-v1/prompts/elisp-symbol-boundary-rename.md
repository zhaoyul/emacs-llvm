Rename the Emacs Lisp function `invoice-total` to `invoice-subtotal` in `invoice.el`.

Update executable references, including the function object used by `render-invoice`. Do not change the string or comment containing `invoice-total`, and do not change the qualified symbol `billing/invoice-total`. Preserve behavior and keep the file structurally valid. Do not modify `DO_NOT_TOUCH.txt`.
