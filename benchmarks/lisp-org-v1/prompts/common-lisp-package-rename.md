Rename the exported Common Lisp function `bench.math:sum` to `bench.math:add`.

Update package exports, the definition, internal executable references, and the external call in `src/app.lisp`. Preserve package qualification, strings, and comments. Keep the system loadable and do not modify `DO_NOT_TOUCH.txt`.
