Rewrite only the `Parent` subtree in `notes.org`.

Rename it to `Delivery Plan`, give it tags `alpha11` and `agent`, set property `OWNER` to `Kevin`, and replace the parent's own body with `Updated body for the delivery plan.`. Preserve the `Child` heading and its text, preserve the entire `Unrelated` subtree, and keep the document title. Do not modify `DO_NOT_TOUCH.txt`.
